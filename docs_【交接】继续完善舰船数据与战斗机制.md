# 交接文档：继续完成《无尽的拉格朗日》AI 战术顾问 —— 舰船详细数据 + 战斗机制 + 计算逻辑

> **用途**：把这份文档整份交给另一个对话，对方看完即可接手继续干活。
> **写于**：2026-09-18　**当前状态**：模拟器已能正常跑完战斗；模块武器已补齐 92 个变体/126 门；剩余大量数据与机制待补。

---

## 0. 一句话交接

这是一个**纯前端静态**的《无尽的拉格朗日》AI 战术顾问（另有一个 FastAPI 后端做代理），核心是三块：
**① AI 多 Agent 问答　② 战舰配队页（fleet.html，同时充当"数据校验器"）　③ 战斗模拟器（simulator.html，纯前端战斗引擎）**。
当前最大的未完成项是：**舰船详细数据（尤其模块武器/拦截/闪避/防空）大量缺失**，以及**战斗引擎还有多个机制未实现**。

---

## 1. 目录 / 仓库 / 部署（必须先搞清楚，否则会改错地方）

| 目录 | 角色 | 仓库 |
|---|---|---|
| `C:\Users\Administrator\Desktop\拉格朗日智能体3` | **工作源**（在这里改） | 非 git 仓库 |
| `C:\Users\Administrator\Desktop\lglr.html` | **静态部署源**（GitHub Pages） | `git@github.com:obxx947/lglr.git`（SSH） |
| `C:\Users\Administrator\Desktop\拉格朗日智能体` | **后端**（FastAPI，端口 3000） | `git@github.com:obxx947/obxx947-lagrange-skill-evolution-studio.git` |
| `C:\Users\Administrator\Desktop\拉格朗日智能体3\web` ← 不对，后端静态副本在 `拉格朗日智能体\web\` | 后端托管的静态副本 | 同上 |

**改完必须同步三份**（否则用户看不到）：`拉格朗日智能体3/` → `lglr.html/` + `拉格朗日智能体/web/`，然后 `git add <具体文件>`（**禁止 `git add -A`**）→ commit → `git push origin main`。
线上地址：`https://obxx947.github.io/lglr/`（改完让用户 **Ctrl+Shift+R 强刷**）。

---

## 2. 权威数据文件地图（哪些是"真"，哪些是旧副本）

| 文件 | 内容 | 状态 |
|---|---|---|
| `拉格朗日智能体3/data/ship_database.json` | **舰船库·唯一数据源**（196 艘） | ✅ 权威 |
| `data/knowledge/舰船资料1~187.md` | **每艘船的详细武器资料**（187 份） | ✅ 权威（但自身有错，见 §6） |
| `data/knowledge/舰船基础信息.md` | 服役上限/人口/模块规则（原始文本版） | 系统提示词指定它为权威 |
| `data/knowledge/舰船站位-*.md`（4 份） | 站位（前/中/后排） | ✅ |
| `data/knowledge/A资料1~712.md`、`实例*.md`、`战斗机制.md`、`黑话.md` | 知识库其余部分（共 1129 个 md） | ✅ |
| 桌面 `数据/战斗机制.md` + `知识库备份-20260808/原版-old/lagrange_docs/战斗机制.txt` | **战斗机制权威文档**（含数值验算例题） | ✅ 用户提供 |
| 桌面 `舰船资料向量拆解/`（17 个 txt） | 187 份 md 的**原始来源**（按舰种分） | ✅ |
| 桌面 `拉格朗日_舰船人口服役_AI查询/` | 给 AI 用的精准查询包（ship_data.json / 对比表 md / 超主力模块 md） | ✅ 已同步 |
| ⚠️ `拉格朗日智能体/lagrange_docs/ship_database.json` | **旧版 193 艘**（多 FSV380、缺 雷火之辉/FSV830/天枪×2） | 待用户决定是否同步 |
| ⚠️ `拉格朗日小程序/utils/ship_db.js` | **旧版 193 艘** | 同上 |
| 桌面 `知识库2/`（737 json） | A资料清洗版，**不是舰船数据** | — |

**生成/校验脚本**：`拉格朗日智能体3/build_rag_index.mjs`（重建向量索引，读 `lglr.html/data/knowledge`，输出 `rag_index.json` + `kb_corpus.json` 到 3 处）。
⚠️ **重建要跑约 17 分钟，不要加 `timeout`**（被中途杀掉会全丢，且管道会让退出码误报为 0）。

---

## 3. `ship_database.json` 结构（含本次新增字段）

数组，196 条。单条：
```jsonc
{
  "id": "uranus-spear", "name": "乌拉诺斯之矛", "variant": "", "type": "battlecruiser",
  "size": "large", "position": "前排",          // position: 前排/中排/后排，战机=aircraft
  "hp": 189493, "physicalArmor": 240, "energyArmor": 5,
  "commandValue": 35, "serviceLimit": 6,        // 人口 / 服役上限
  "speed": {...}, "ratings": {...},             // ⚠️ 代码只用它们显示，不参与战斗
  "airSlots": { "base": [], "byModule": { "B2": [{ "kind":"corvette","size":"corvette","cap":3 }] } },
  "airSize": "large" | "small",                 // 战机/护航艇用：能否进"仅中小型"的载机位
  "modules": {
    "M": { "type":"moduleGroup", "name":"M舰首主武器", "variants": {
        "M1": { "name":"舰首轨道炮系统", "weapons":[ ... ] },   // ← 本次补齐的就是这里
        "M2": { ... } } },
    "A": { ... }, "_systems": { ... }           // "_" 开头会被代码跳过
  }
}
```
**武器对象**（本次由资料生成，带 `_src` 溯源）：
```jsonc
{ "name":"红宝石-BR-1950C轨道炮", "dmgType":"physical|energy", "weaponType":"direct|projectile",
  "singleDmg":3250, "ammo":1, "attacks":1, "atkDuration":0, "lockTime":0, "cooldown":10.2,
  "priority":"大型舰船", "dpm":{ "antiShip":19058 },
  "targets":[{ "types":["大型舰船"], "hitMin":70, "hitMax":100 }],
  "antiAirType":"counter|area", "crit":true, "_src":"舰船资料131.md" }
```
> `targets[].types` **可以写中文**（`大型舰船/小型舰船/舰载机`），代码的 `matchesType()` 直接支持。

---

## 4. 战斗引擎现状（`simulator.html`）

### 4.1 已实现且与用户《战斗机制》文档**一致**
- 调校系数 **1.3**；物理=**减法**、能量=**乘法**；**不破防只吃 10%**
- 能量抗性 ≥100% → 0 伤害
- 命中 = `(hitMin + rand×(hitMax−hitMin))/100 × (1+(lockEfficiency−evasion)/100)`，clamp 0.01~0.99
- **锁定与冷却并行**；**同目标不重复锁定**；目标死亡带剩余发数重新锁敌（不重置冷却）
- **直射武器不会被拦截**（投射才拦）；拦截按 `Π(1−r/100)` 累乘（= 文档的 `1−(1−x)^n(1−y)^m`）
- 系统破坏效率 **高60%/中40%/低20%**、系统伤害 `单发×1.5×1.3`、25 秒修复、各系统维修次数（指挥3/自修2/其它0）
- **只有动力系统**被破坏才扣 5% 最大结构
- **母舰被摧毁 → 其舰载机一起被摧毁**（靠实例级 `carrierInstId`）
- 大盾旗舰全场 ×0.7；**被护航舰在护航存活时伤害=0**；分伤候选收缩 `max(1, round(n/2.5))`
- 轰炸模式距离：每偏离 15 吉米 ±2% 命中
- 结构值可按强化 +%（`maxHp = hp×(1+hpBonus/100)`）

### 4.2 **未实现**（按用户文档应有，属于本次交接的重点）
| 缺口 | 文档依据 | 需要的数据 |
|---|---|---|
| **拦截机制整体空转** | 「拦截率 = 1−(1−x)^n(1−y)^m」 | 每艘舰的 `interceptRate` + `interceptType`(global/sameRow/自身) —— **全库 0 条** |
| **闪避** | 「命中 = 基础命中×(1+命中加成−敌方闪避)」 | 每艘的 `evasion` —— **全库 0 条** |
| 反拦截 | 「最终拦截 ×(1−敌方反拦截)」 | 反拦截百分比 —— 无字段 |
| **防空三态**（反击/区域/主动）+ 舰载15%/机载60%命中 | 文档第四节 | 数据里有 `antiAirType`(counter/area，136 门) 但**代码不读**；主动防空无标记 |
| 系统**独立血量**（"绝育"玩法，如大帝主武器 22000） | 文档第二节 | 各系统血量 —— 全库 0 条 |
| 打系统的武器打结构 **×0.8** 系数 | 「可攻击系统的武器打结构有系数，不全是0.8」 | 系数不在数据里 |
| **策略系数**（乘算/加算） | 「(1+策略系数)」 | 数据有 `strategies`（6 门）但代码不读 |
| 两大战机模式（**独立作战 / 往复打击**）+ 飞行时间 | 文档第三节「每1吉米往复时间+2秒」 | 数据有 `flightMode` 但不读；无 去程/返程时间 |
| 母舰机库加成 | 「不同航母的机库对舰载机有不同加成」 | 无数据 |
| 旗舰技能（**目前只实现了大盾**） | 「旗舰属性…照着那样办」 | 各舰旗舰效果文本 —— 用户说"以后发过来" |
| 面板 DPM 未按用户实测式重算 | 用户实测：`(单发−抵抗+强化)×安装数×轮次×每次×60÷(持续+冷却)` | 页面目前直接用数据里的 `dpm` 字段 |
| 速度/距离不影响交战 | 文档未明确要求 | 代码不读 `speed`，无先后手 |

---

## 5. 本次已完成的"模块武器补齐"（**可复用的方法，务必先看**）

**问题**：超主力武器存在**模块变体**里，而 233 个变体**全部没有 `weapons`** → 17 艘超主力在战斗里**零输出**，表现为"打上万秒、巨大劣势"。

**数据来源**：`data/knowledge/舰船资料N.md`（187 份），每份含每个模块变体的：
`#### M1 舰首轨道炮系统（原装）` → `- 2×BG-1850重型火炮` → `- 每分钟伤害：9400，伤害类型：实弹，优先目标：大型舰船，单发伤害：950，特性：反击防空`

**提取规则（已实现并验证）**：
1. **周期推导**：`cooldown = 单发伤害 × 数量 × 60 ÷ 每分钟伤害`（由用户文档的"面板分钟伤害"公式反推）→ **实测每条武器输出与面板 DPM 误差仅 1~3%** ✅
2. `优先目标` → `types`（大型舰船/小型舰船/舰载机）
3. 命中率：资料有 `##### 攻击序列+命中率` 表就用表；无表按优先目标推断（**大 70~100 / 小 50~70 / 舰载机 10~20**，依据文档"舰载防空15%、机载60%"）
4. `伤害类型` 实弹→`physical`、能量→`energy`；**直射/投射按武器名推断**（导弹/鱼雷/无人机→`projectile`，其余→`direct`）
5. `特性`：反击防空→`antiAirType:'counter'`、区域防空→`'area'`、暴击→`crit:true`
6. 每门武器写 `_src` 标明来源资料文件，便于复核

**结果**：填充 **92 个变体 / 126 门武器**；有武器的舰船 ~90 → **183 艘**。
**两种必须兼容的行格式**（我踩过坑）：
- `- 2×BG-1850重型火炮`（带数量×）
- `- 12联装反舰导弹系统`（**无 × 前缀**，数量在名字里）
- 只给「每分钟伤害」不给「单发伤害」的 → 按 DPM 折算（1发/秒）兜底
- 解析必须"武器名行 → 紧邻字段行"配对，**不能只匹配 `N×`**

**未覆盖（仍缺武器的 19 艘）**：`leihuo-hui`(雷火之辉)、`FSV830`、`tianqiang-tactical`、`tianqiang-heavy`、`yueshenxing-A`(阅神星Ⅰ)、`yuhai-B`、`ruby-B`、`wildfire`、`ship-ato21c`、`peidao`、`niuwa`、`haishi`、`linxiao`、`shalong`、`andersen`、`sc002`、`lizhi-te` + 前面几艘的同型变体。
**另有**：`永恒风暴 D1/D2` 资料里写"（无细分数值）"。

---

## 6. 已知数据问题清单（**待用户确认后再改**）

| 问题 | 说明 |
|---|---|
| **猎兵级-重型载机巡洋舰 hp** | 资料写 **1120**，库里 **76190**。同型船体"狩猎者级"资料里也是 76190 → 判定资料笔误，**暂未改，等用户确认** |
| 12 份资料的「舰船类型」写错 | 诺玛330、迅捷级×3、卡利斯托×2、XT-8、刺水母、诺玛M470、云海、**安东塔斯**、**雷火之星**（写成"驱逐舰"）→ **资料错、库对** |
| 7 份资料在库里找不到对应 | 含已删的 FSV380、`ATO21`(应为 AT021)、`阅神星I级`(库内是罗马数字Ⅰ) 等，属源文件拆分串行 |
| 4 艘连 `hp` 都没有 | `leihuo-hui`、`FSV830`、`tianqiang-tactical`、`tianqiang-heavy`（代码里有兜底不会 NaN，但数值是编的） |
| 旧副本 193 | `拉格朗日智能体/lagrange_docs/ship_database.json`、`拉格朗日小程序/utils/ship_db.js` |
| 站位仍缺 | 战机/护航艇的 position 统一为 `aircraft`（不参与前中后排） |

---

## 7. 用户明确的硬约束（**必须遵守**）

1. **不得在未经同意的情况下修改 `data/system_prompt.md`**（系统提示词）
2. **只用"拉格朗日智能体3"当工作源**；`git add` **禁止 `-A`**（只加具体文件）
3. **后端只允许 GLM**，不许用用户的 DeepSeek/其它个人 key（DeepSeek 仅用于用户明确授权的离线清洗/测试）
4. **技术评估必须绝对诚实**：不玩笑、不编造、不知道就说不知道、**不许把"没实现"说成"已实现"**
5. 用户说"**以战舰配队为准**"：配队页是数据权威（人口/服役/载机位/站位一律以舰船库重算）

---

## 8. 测试与验证方法（**强烈建议照做，别只读代码**）

### 8.1 浏览器回归（本机 Edge + puppeteer-core）
```bash
cd C:/Users/Administrator/Desktop/拉格朗日智能体3
python -m http.server 3888 --bind 127.0.0.1     # 起静态服务
node <脚本>.js                                   # 脚本里用 puppeteer-core 打开 http://127.0.0.1:3888/xxx.html
```
- 现成回归脚本（**10 套、151 项，全绿**）在 `拉格朗日智能体/test/`：
  `fleet_ui_regression.js`(32) / `fleet_check_regression.js`(39) / `simulator_fleet_regression.js`(21) /
  `fleet_multiconfig_regression.js`(17) / `position_regression.js`(9) / `aircraft_count_regression.js`(7) /
  `fleet_count_inflation_regression.js`(7) / `fleet_text_roundtrip_regression.js`(7) /
  `strengthen_and_module_regression.js`(7) / `battle_mechanics_regression.js`(7)
- 脚本开头若带注释头，**跑之前要把头两行注释切掉**（我保存时加了说明头）
- ⚠️ **必须挂 `p.on('dialog', d=>d.accept())`**，否则 `confirm()` 会卡死脚本
- ⚠️ 测"是否开火/是否掉血"要用**目标 HP 变化**，不要比较 `cooldown` 浮点数（`10.0−0.1×100` 会留 1e-15）
- ⚠️ 长任务要给 `puppeteer.launch({protocolTimeout: 300000})`，默认 180s 会超时

### 8.2 直接跑战斗（不开 UI，最快）
```js
// 在页面里 evaluate：
prepareBattle();
let t=0; while(!battleState.ended && t<200000){ processBattleTick(0.1); t+=0.1; }
```
**当前基准**：12v12（4大盾+4大矛+4大帝、每方 2,104,196 结构）→ **756~967 秒结束**，镜像战 6 次各胜 3（无单边偏袒）。

### 8.3 看真实浏览器（用户说"还在"时用这招，比盲猜有效得多）
```python
# ctypes EnumWindows + GetWindowTextW 抓窗口标题（PowerShell 在本机不可用）
# PIL ImageGrab.grab(bbox=GetWindowRect(hwnd)) 截图
# pyautogui 点击：屏幕坐标 = (bbox.left + 图像x, bbox.top + 图像y)；pyautogui.scroll 可滚 Chromium
```

---

## 9. 建议的下一步（按优先级，都带数据格式）

### P0 —— 让拦截/闪避机制活过来（代码已就绪，只差数据）
往 `ship_database.json` 每艘加：
```jsonc
"interceptRate": 27,            // 单舰拦截率 %（文档例：雷火之星-B2=27%、光追级防空型=23%）
"interceptType": "global",      // global=全域 / sameRow=同排 / 省略=自身
"evasion": 15                   // 闪避 %
```
代码位置：`simulator.html` 的 `executeShot()` 拦截段已按 `interceptRate/interceptType` 实现，加数据即生效。

### P1 —— 补全剩余 19 艘船的模块武器
按 §5 的规则，把缺的船从 `舰船资料*.md`（或用户新提供的资料）补进 `ship_database.json`；重点：**FSV830、天枪×2、雷火之辉**（这 4 艘是超主力/新增船，目前 0 武器）。

### P2 —— 系统独立血量（"绝育"玩法）
给每个 `modules[槽]` 或系统加 `hp`（如大帝主武器 22000、动力 20000、机库 26500），把现在"概率直接摧毁"改成"累积伤害到系统血量归零才摧毁"；并实现 `subSystemTargets` 的**效率分流**（低20%/中40%/高60%）。

### P3 —— 防空三态
读数据里的 `antiAirType`：`counter`=反击防空（自身/同排被攻击时反击）、`area`=区域防空（可打以同排为目标的空中单位）；再加"主动防空"标记。舰载防空基础命中 **15%**、机载 **60%**（文档明确）。

### P4 —— 两大战机模式 + 飞行时间
`flightMode` = `independent`（独立作战：飞一次，持续锁定攻击）/ 往复打击（打完返航补给再出击）；**每 1 吉米往复时间 +2 秒**；轰炸距离命中 ±2%/吉米（后者已实现）。

### P5 —— 其余
策略系数（读 `weapons[].strategies`）、打系统武器打结构 ×0.8、机库加成、旗舰技能（**待用户提供各舰旗舰效果**）、面板 DPM 按用户实测式重算。

### 数据格式约定（给用户提数据时照这个问）
- **舰船**：`名称 | 人口 | 服役上限 | 结构值 | 物理护甲 | 能量护甲 | 站位 | 拦截率 | 闪避`
- **模块**：`舰名 | 槽位(如B) | 变体码(如B2) | 变体名 | 是否原装`
- **武器**：`模块变体 | 武器名 | 数量 | 单发伤害 | 每分钟伤害 | 伤害类型 | 直射/投射 | 优先目标 | 特性(暴击/反击防空/区域防空…)`
- **系统血量**：`舰名 | 系统名 | 血量 | 被毁效果`
- **旗舰技能**：`舰名 | 技能名 | 效果（含数值）`

---

## 10. 相关文档（都在桌面 + 仓库 `docs/`）
- `机制对照表-文档vs代码.md` —— 你的机制文档 vs 代码，逐条标注 ✅一致/🔧已修/⚠️缺/📉数据缺口
- `战斗模拟器机制与计算公式.md` —— 引擎全部公式与流程
- `模块武器补齐报告.md` —— 本次补齐的根因/方法/效果
- `舰船数据源审计.md` —— 所有舰船数据源的数量比对与问题清单

---

## 11. 优先级建议（如果时间有限）
**先做 P0（拦截+闪避）和 P1（补 4 艘关键船的武器）** —— 这两项投入小、对战斗结果影响最大；P2 系统血量是"绝育流"玩法的前提；P3/P4 属于体验还原。
**每改一次数据都要跑一遍 `test/` 里的 10 套回归**，并同步三副本 + 推送。
